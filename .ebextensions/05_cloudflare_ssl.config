# Configuração para usar CloudFlare SSL

# 1. Cadastre-se no CloudFlare (gratuito)
# 2. Adicione seu domínio maestrocommunications.shop
# 3. Altere os nameservers no Hostinger para os do CloudFlare
# 4. Configure SSL no CloudFlare (Flexible SSL é suficiente)

# Configurações Django para CloudFlare SSL
option_settings:
  aws:elasticbeanstalk:application:environment:
    # CloudFlare passa HTTPS via header
    USE_CLOUDFLARE_SSL: "true"
    SECURE_PROXY_SSL_HEADER_NAME: "HTTP_CF_VISITOR"
    SECURE_PROXY_SSL_HEADER_VALUE: "https"
    
    # Configurações de segurança
    SECURE_SSL_REDIRECT: "false"  # CloudFlare faz o redirect
    SESSION_COOKIE_SECURE: "true"
    CSRF_COOKIE_SECURE: "true"
    SECURE_CONTENT_TYPE_NOSNIFF: "true"
